import React from 'react';
import ReactDOM from 'react-dom';
import './styles/styles.css';

ReactDOM.render(
  <h1>
    Hello from index.js with
    React and Babel and CSS in
    RED
  </h1>,
  document.querySelector(
    '#fromjs'
  )
);
